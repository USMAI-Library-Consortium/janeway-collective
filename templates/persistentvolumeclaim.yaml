{{- if and .Values.persistence.enabled (not .Values.persistence.existingClaim) -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "janeway.persistence.claimName" . }}
  namespace: {{ .Release.Namespace }}
  {{- $labels := include "common.tplvalues.merge" ( dict "values" ( list .Values.persistence.labels .Values.commonLabels )) }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" $labels "context" $ ) | nindent 4 }}
    {{ include "common.labels.component.app" . }}
  {{- if or .Values.commonAnnotations .Values.persistence.annotations -}}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.persistence.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $ ) | nindent 4 }}
    {{ if .Values.persistence.keep -}}
    helm.sh/resource-policy: keep
    {{ end -}}
  {{- end }}
spec:
  accessModes: {{- range .Values.persistence.accessModes }}
    - {{ .  | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.size | required "Please set Janeway storage size in persistence.size." | quote }}
  {{- if (or .Values.global.defaultStorageClass .Values.persistence.storageClass)  }}
  storageClassName: {{ default .Values.global.defaultStorageClass .Values.persistence.storageClass }}
  {{- end }}
{{- end }}